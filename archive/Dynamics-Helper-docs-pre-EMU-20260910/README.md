# Private Historical Archive

This is a private historical snapshot, not a migration package, migration input,
active project documentation, or authorization for future operations. Do not
publish it or import it into an EMU repository as active guidance.

Canonical source: `C:\MyWorkbench\Repository\Dynamics-Helper`.
Snapshot date: 2026-09-10.

- 49 selected documents have disposition `remove`.
- 4 additional complete snapshots have disposition `retain-summary`.
- These dispositions label intended later documentation cleanup only. This
  operation did not delete, edit, truncate, or summarize any source file.
- All 53 copies retain their original repository-relative hierarchy, creation
  time, and last-write time; destination attributes retain source attributes
  with read-only added. Source contents and recorded metadata were unchanged.
- The current locally modified `docs/session-handoff-2026-07-15.md` was copied
  in full from the working tree, not from HEAD, including local updates.
- `manifest.json` records `relativepath`, `bytes`, lowercase SHA-256 `hash`,
  and `disposition` for each of the 53 source documents.
- `archive-snapshot.zip` contains the same 53 documents and `manifest.json`
  (54 entries). This explanatory README was added afterward and is not in
  the ZIP or the source-document manifest.
- All 53 source-before/source-after/copy/ZIP hashes matched, with zero
  mismatches. Read-only source handles denied write/delete sharing during
  copying and verification. Source and destination paths were checked for
  reparse points. No existing archive target was overwritten.
- No Git add, commit, push, network access, or product tests were performed.

Verified SHA-256 values at archive creation:

```text
manifest.json        319f75a5e42f290c4310f87d8c7f56931e6528f0710862057f87880d1a826848
archive-snapshot.zip 58388442c8714f506298980268f607dc5b1745c12ba7fdfa3a71ee4cb06f9d0b
```

Hashes verify byte identity, not authenticity or authorization. Preserve the
full snapshots even if later authorized work removes or summarizes active
documents. Historical approvals and handoffs do not grant new authority.
