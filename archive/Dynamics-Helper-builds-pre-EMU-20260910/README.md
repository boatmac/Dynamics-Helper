# Local Build History

This private directory retains historical local build artifacts outside workspace exports.
The only canonical workspace is `C:\MyWorkbench\Repository\Dynamics-Helper`.

`manifest-before.json` records every archived file's original size and SHA-256 before movement.
`operations.jsonl` and `summary.json` record the moves and matching post-move verification.
