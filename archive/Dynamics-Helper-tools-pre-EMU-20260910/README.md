# Private Historical Tools Archive

This archive may contain private or sensitive historical material. Keep it local
and private; do not publish or upload it.

Historical scripts are reference-only and must NOT be executed. They must NOT be
included in the new EMU tooling, test execution scope, or deployment inputs.

This operation copied 20 legacy tools/documents and one safety-manifest snapshot
from the canonical workspace, preserving their relative paths. It deleted and
modified no source files. No archived script was executed.

manifest.json records byte counts, SHA-256 hashes, dispositions, and matching
source-before/source-after hashes. archive-copy denotes the 20 legacy copies;
retain-modified denotes the tests/test-safety-manifest.json snapshot and does not
authorize changing or deleting its source. Any later source cleanup is a separate
operation owned by the primary task.
