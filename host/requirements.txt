# Direct dependency: the GitHub Copilot SDK (a JSON-RPC client for the
# Copilot CLI — see AGENTS.md § 9.5). Range allows 1.0.x patch uptake
# without auto-jumping a breaking minor. Upgraded 0.3.0 → 1.0.5 on
# 2026-07-03 (docs/sdk-upgrade-2026-07-1.0.5.md).
github-copilot-sdk>=1.0.5,<1.1

# Transitive dependencies of github-copilot-sdk 1.0.5, pinned for
# reproducible PyInstaller builds. Regenerate with `pip freeze` after
# bumping the SDK pin. DH's own code uses only stdlib urllib.request, so
# there is no direct `requests` dependency (the 0.3.0-era requests /
# urllib3 / charset-normalizer stack was dropped — 1.0.5 uses httpx).
annotated-types==0.7.0
anyio==4.14.1
certifi==2026.1.4
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.11
pydantic==2.12.5
pydantic_core==2.41.5
python-dateutil==2.9.0.post0
six==1.17.0
typing-inspection==0.4.2
typing_extensions==4.15.0
