# Direct dependency: the GitHub Copilot SDK (a JSON-RPC client for the
# Copilot CLI). See docs/sdk-integration.md for current contracts and
# docs/sdk-upgrade-workflow.md for dependency review and verification.
github-copilot-sdk==1.0.13

# Transitive dependencies retained for github-copilot-sdk 1.0.13, pinned for
# reproducible PyInstaller builds. Regenerate with `pip freeze` after
# bumping the SDK pin. DH's own code uses only stdlib urllib.request, so
# there is no direct `requests` dependency; the SDK uses httpx.
annotated-types==0.7.0
anyio==4.14.1
certifi==2026.1.4
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.11
pydantic==2.12.5
pydantic_core==2.41.5
python-dateutil==2.9.0.post0
six==1.17.0
typing-inspection==0.4.2
typing_extensions==4.15.0
